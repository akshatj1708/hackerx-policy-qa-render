# Web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-dotenv==1.0.0

# LangChain
langchain==0.1.12
langchain-core==0.1.53
langchain-community==0.0.29
langchain-huggingface==0.0.3

# Pinecone client
pinecone-client==5.0.1
langchain-pinecone==0.2.11

# Google GenAI integration (lighter than local models)
langchain-google-genai==0.0.8

# Lightweight embedding model
sentence-transformers>=2.6.0

# Core dependencies with specific versions to minimize size
numpy==1.26.4
pydantic==2.7.4

# Document processing
pypdf==3.17.4

# Required by sentence-transformers
transformers>=4.36.2
tokenizers>=0.19.1

# Optimized PyTorch (CPU only, minimal)
torch==2.1.2 --index-url https://download.pytorch.org/whl/cpu
